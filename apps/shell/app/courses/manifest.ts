import type { ElementConfig } from "@shane/types";

const manifest: ElementConfig = {
  id: "courses",
  symbol: "Co",
  name: "Courses",
  category: "projects",
  type: "internal",
  route: "/courses",
  status: "live",
  description: "Interactive course catalog: AI-classified lectures with covers, ratings, and reviews.",
};

export default manifest;
