import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Upload trip — Shane",
};

export default function NewTripLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
