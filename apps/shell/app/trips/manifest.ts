import type { ElementConfig } from "@shane/types";

const manifest: ElementConfig = {
  id: "trips",
  symbol: "Tr",
  name: "Trips",
  category: "data-tracking",
  type: "internal",
  route: "/trips",
  status: "live",
  description: "Travel itineraries — upload an HTML file, get a page.",
};

export default manifest;
